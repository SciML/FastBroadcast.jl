name: Documentation
on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master
    tags: "*"
jobs:
  build:
    uses: "SciML/.github/.github/workflows/documentation.yml@v1"
    secrets: "inherit"
