var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"The public interface of FastBroadcast.jl consists of the @.. macro and the Serial and Threaded threading-dispatch types.","title":"API"},{"category":"section","location":"api/#The-@..-macro","page":"API","text":"","title":"The @.. macro"},{"category":"section","location":"api/#Threading-dispatch-types","page":"API","text":"","title":"Threading dispatch types"},{"category":"macro","location":"api/#FastBroadcast.@..","page":"API","text":"@.. [thread=false] [broadcast=false] expr\n\n@.. turns expr into a broadcast-like expression, similar to @.. It additionally provides two optional keyword arguments:\n\nthread: Defaults to false. Controls multithreading via Polyester.jl.   Accepts Serial(), Threaded(), false, or true. Using the type-based   Serial()/Threaded() is preferred as it enables compile-time dispatch and   avoids invalidations when Polyester is loaded. Requires using Polyester   for threaded execution.   This only works if broadcast=false.\nbroadcast: Defaults to false. If true, it will broadcast axes with dynamic   runtime sizes of 1 to larger sizes, if false only sizes known to be 1   at compile time will be supported, i.e. axes such that   ArrayInterface.known_length(typeof(semi_static_axes(x,i))) == 1 will   be broadcast. Note that this differs from base broadcasting, in that   base broadcasting only supports broadcast=true.\n\n\n\n\n\n","title":"FastBroadcast.@.."},{"category":"type","location":"api/#FastBroadcast.Serial","page":"API","text":"Serial()\n\nThreading dispatch type for @... When passed as thread = Serial(), uses serial broadcasting (the default). Using a type instead of false allows compile-time dispatch.\n\n\n\n\n\n","title":"FastBroadcast.Serial"},{"category":"type","location":"api/#FastBroadcast.Threaded","page":"API","text":"Threaded()\n\nThreading dispatch type for @... When passed as thread = Threaded(), enables Polyester-based multithreaded broadcasting. Using a type instead of true allows compile-time dispatch and prevents invalidations when Polyester is loaded (the Serial code path never references threaded methods).\n\n\n\n\n\n","title":"FastBroadcast.Threaded"},{"category":"section","location":"#FastBroadcast.jl","page":"Home","text":"FastBroadcast.jl exports @.. that compiles broadcast expressions into loops that are easier for the compiler to optimize.\n\nusing FastBroadcast\n\nfunction fast_foo9(a, b, c, d, e, f, g, h, i)\n    @.. a = b + 0.1 * (0.2c + 0.3d + 0.4e + 0.5f + 0.6g + 0.6h + 0.6i)\n    nothing\nend\n\nIt is important to note that FastBroadcast does not speed up \"dynamic broadcast\", i.e. when the arguments are not equal-axised or scalars. For example, dynamic broadcast happens when the expansion of singleton dimensions occurs.","title":"FastBroadcast.jl"},{"category":"section","location":"#Threading","page":"Home","text":"The macro @.. accepts a keyword argument thread to control whether the broadcast should use multithreading via Polyester.jl (disabled by default). Start Julia with multiple threads to benefit from this, and load Polyester to activate the threaded code path.\n\nusing FastBroadcast, Polyester\n\nfunction foo_parallel!(dest, src)\n    @.. thread = true dest = log(src)\nend\n\nThe thread argument accepts true/false or the exported types Serial()/Threaded(). When the threading choice is stored in a type-parameterized struct (e.g. an algorithm configuration), using Serial()/Threaded() enables compile-time dispatch and avoids invalidations when Polyester is loaded.\n\nSee the API page for the full public interface.","title":"Threading"}]
}
